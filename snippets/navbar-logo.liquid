<a 
  class="navbar-logo navbar-brand" 
  href="{{ routes.root_url }}">

    <img
      class="img-fluid navbar-logo-default"
      src="{{ settings.logo | image_url: height: 200 }}" 
      alt="{{ shop.name }}"
      width="{{ 100 | times: settings.logo.aspect_ratio | round }}" 
      height="100"
      loading="lazy">
  
</a>